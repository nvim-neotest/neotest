#!/usr/bin/env bash
# Requires busted + nlua (e.g. `luarocks test --local`, or on Nix:
# nix-shell -p luajitPackages.busted luajitPackages.nlua luajitPackages.nvim-nio)
cd "$(dirname "$0")/.." || exit 1
exec busted "$@"
